import React from 'react';
import './Header.css'; // Import the CSS file for Header

const Header = () => {
  return (
    <header>
      <h1>Trucker's Daily Log</h1>
    </header>
  );
};

export default Header;